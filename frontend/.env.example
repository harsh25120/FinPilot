# Copy this file to .env and adjust if your backend runs somewhere else:
#   cp .env.example .env

# Base URL of the FinPilot API (must include the /api/v1 prefix).
VITE_API_BASE_URL=https://finpilot-api-ypf6.onrender.com/api/v1